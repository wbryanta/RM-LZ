using Verse;

namespace LandingZone
{
    public class LandingZoneSettings : ModSettings
    {
        public override void ExposeData()
        {
            base.ExposeData();
            // Future settings go here (e.g., default filter states, telemetry opt-in flags).
        }
    }
}
